name: 🚀 Build & Release Gestion Financière Little

permissions:
  contents: write
  actions: read

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build et Release multi-plateforme
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - name: 🧰 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: 📦 Copier le dossier Tesseract depuis le dépôt
        if: runner.os == 'Windows'
        run: |
          if [ -d "tesseract" ]; then
            echo "📂 Dossier tesseract trouvé dans le dépôt"
            mkdir -p dist/tesseract
            cp -r tesseract/* dist/tesseract/
            echo "✅ Tesseract local copié dans dist/"
          else
            echo "⚠️ Aucun dossier tesseract trouvé dans le dépôt"
          fi
        shell: bash

      
      - name: 📝 Créer le dossier dist
        run: |
          mkdir -p dist
          echo "✅ Dossier dist créé"
        shell: bash

      - name: 📝 Créer script de lancement pour Linux/macOS
        if: runner.os != 'Windows'
        run: |
          cat > dist/run.sh << 'RUNSCRIPT'
          #!/bin/bash
          # =====================================================
          #  🚀 Script de lancement : Gestion Financière Little
          # =====================================================

          SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
          cd "$SCRIPT_DIR"

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "  🚀 Gestion Financière Little (Linux/macOS)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📂 Répertoire de travail: $SCRIPT_DIR"
          echo ""

          MAIN_FILE="gestiolittle.py"
          VENV_DIR="$SCRIPT_DIR/.little_finance_env"

          # --- Vérification du fichier principal ---
          if [ ! -f "$MAIN_FILE" ]; then
              echo "❌ ERREUR: $MAIN_FILE introuvable dans $SCRIPT_DIR"
              exit 1
          fi
          echo "✅ Fichier principal trouvé: $MAIN_FILE"
          echo ""

          # --- Détection distribution Linux ---
          detect_linux_distro() {
              if [ -f /etc/os-release ]; then
                  . /etc/os-release
                  echo "$NAME"
              elif command -v lsb_release &>/dev/null; then
                  lsb_release -d | cut -f2
              else
                  echo "Unknown"
              fi
          }
          DISTRO=$(detect_linux_distro)
          echo "🐧 Distribution détectée: $DISTRO"
          echo ""

          # --- Vérification Python ---
          if ! command -v python3 &>/dev/null; then
              echo "📦 Python3 manquant — installation automatique..."
              case "$DISTRO" in
                  *"Ubuntu"*|*"Debian"*|*"Mint"*|*"MX"*)
                      sudo apt-get update && sudo apt-get install -y python3 python3-pip python3-venv ;;
                  *"Fedora"*|*"Red Hat"*|*"CentOS"*)
                      sudo dnf install -y python3 python3-pip python3-virtualenv ;;
                  *"Arch"*|*"Manjaro"*)
                      sudo pacman -S --noconfirm python python-pip python-virtualenv ;;
                  *"Darwin"*|*"macOS"*)
                      if ! command -v brew &>/dev/null; then
                          /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
                      fi
                      brew install python@3.11 ;;
                  *)
                      echo "⚠️ Impossible d’installer automatiquement Python sur cette distribution."
                      exit 1 ;;
              esac
          fi
          echo "✅ Python détecté: $(python3 --version)"
          echo ""

          # --- Création ou réutilisation du venv ---
          if [ ! -d "$VENV_DIR" ]; then
              echo "📦 Création d’un nouvel environnement virtuel..."
              python3 -m venv "$VENV_DIR" || {
                  echo "❌ Échec de création du venv"
                  exit 1
              }
              echo "✅ Environnement virtuel créé"
          else
              echo "✅ Environnement virtuel déjà présent"
          fi

          # --- Activation du venv ---
          source "$VENV_DIR/bin/activate" || {
              echo "❌ Impossible d’activer le venv"
              exit 1
          }
          echo "✅ Environnement virtuel activé"
          echo ""

          # --- Vérification et installation des dépendances Python ---
          echo "🔍 Vérification des dépendances Python..."
          if ! python -c "import streamlit" &>/dev/null; then
              echo "📦 Installation initiale des dépendances..."
              python -m pip install --upgrade pip --quiet
              python -m pip install streamlit pandas pytesseract Pillow python-dateutil opencv-python-headless numpy matplotlib pdfminer.six requests --quiet
              echo "✅ Dépendances installées avec succès"
          else
              echo "✅ Dépendances déjà présentes"
          fi

          # --- Vérification de Tesseract ---
          if ! command -v tesseract &>/dev/null; then
              echo "🔍 Tesseract non détecté — tentative d’installation..."
              case "$DISTRO" in
                  *"Ubuntu"*|*"Debian"*|*"Mint"*|*"MX"*)
                      sudo apt-get install -y tesseract-ocr tesseract-ocr-fra ;;
                  *"Fedora"*|*"Red Hat"*|*"CentOS"*)
                      sudo dnf install -y tesseract tesseract-langpack-fra ;;
                  *"Arch"*|*"Manjaro"*)
                      sudo pacman -S --noconfirm tesseract tesseract-data-fra ;;
                  *"Darwin"*|*"macOS"*)
                      brew install tesseract tesseract-lang ;;
              esac
              echo "✅ Tesseract installé avec succès"
          else
              echo "✅ Tesseract détecté: $(tesseract --version | head -n1)"
          fi
          echo ""

          # --- Vérification OpenCV ---
          if ! python -c "import cv2" &>/dev/null; then
              echo "📦 Installation de OpenCV manquant..."
              python -m pip install opencv-python-headless --quiet
              echo "✅ OpenCV installé"
          else
              echo "✅ OpenCV déjà présent"
          fi
          echo ""

          # --- Lancement de l'application ---
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "  ▶️  Lancement de Gestion Financière Little"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "💡 L’application s’ouvrira sur http://localhost:8501"
          echo "💡 Pour arrêter : Ctrl + C"
          echo ""
          sleep 2

          python -m streamlit run "$MAIN_FILE" --server.headless true --server.port 8501

          RUNSCRIPT

          chmod +x dist/run.sh
          echo "✅ Script run.sh créé avec succès (version optimisée)"
        shell: bash

        
      - name: 🧱 Installation des dépendances Python avant le build
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install streamlit pandas pytesseract Pillow python-dateutil opencv-python-headless numpy matplotlib pdfminer.six requests
          echo "✅ Toutes les dépendances nécessaires au build sont installées"

      - name: 🏗️ Compiler les deux versions Windows (Portable + Lite)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Write-Host "🏗️ Compilation Gestion Financière Little (Windows uniquement)..."

          # 🔧 Installation / mise à jour des outils nécessaires
          python -m pip install --upgrade pip
          python -m pip install pyinstaller pyinstaller-hooks-contrib

          Write-Host ""
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          Write-Host "🔹 VERSION PORTABLE (autonome, sans installateur ni questions)"
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          & pyinstaller `
            "app/lancer_gestiolittle.py" `
            --noconfirm `
            --clean `
            --onefile `
            --console `
            --name "GestionFinanciereLittle_Portable" `
            --add-data "app;app" `
            --add-data "tesseract;tesseract" `
            --collect-all streamlit `
            --collect-all pandas `
            --collect-all PIL `
            --collect-all pytesseract `
            --collect-all cv2 `
            --collect-all numpy `
            --collect-all matplotlib `
            --collect-all pdfminer `
            --hidden-import streamlit `
            --hidden-import streamlit.web.bootstrap `
            --hidden-import streamlit.web.server.browser_websocket_handler `
            --hidden-import streamlit.runtime.scriptrunner.script_runner `
            --hidden-import pandas `
            --hidden-import numpy `
            --hidden-import matplotlib `
            --hidden-import pytesseract `
            --hidden-import Pillow `
            --hidden-import cv2 `
            --hidden-import pdfminer `
            --hidden-import pdfminer.six `
            --hidden-import requests `
            --hidden-import python-dateutil 

          Write-Host ""
          Write-Host "✅ Compilation de la version Portable terminée"
          Write-Host ""

          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          Write-Host "📦 Vérification post-compilation (Portable)"
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          Get-ChildItem dist/
          Write-Host ""

          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          Write-Host "🔹 VERSION LITE (utilisant le Python global)"
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          & pyinstaller `
            "app/lancer_gestiolite.py" `
            --noconfirm `
            --clean `
            --onefile `
            --console `
            --name "GestionFinanciereLittle_Lite" `
            --exclude-module pytest `
            --exclude-module setuptools `
            --exclude-module pip `
            --exclude-module wheel `
            --exclude-module test `
            --exclude-module unittest `
            --exclude-module streamlit `
            --exclude-module pandas `
            --exclude-module numpy `
            --exclude-module matplotlib `
            --exclude-module pytesseract `
            --exclude-module Pillow `
            --exclude-module opencv-python-headless `
            --exclude-module cv2 `
            --exclude-module pdfminer `
            --exclude-module pdfminer.six `
            --exclude-module requests `
            --exclude-module python-dateutil 

          Write-Host ""
          Write-Host "✅ Compilation de la version Lite terminée"
          Write-Host ""

          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          Write-Host "📦 Copie des fichiers additionnels"
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item app/configlittle.py dist/ -ErrorAction SilentlyContinue
          Copy-Item app/auto_updater.py dist/ -ErrorAction SilentlyContinue
          Copy-Item app/changelog_viewer.py dist/ -ErrorAction SilentlyContinue
          Copy-Item app/gestiolittle.py dist/ -ErrorAction SilentlyContinue

          if (Test-Path "tesseract") {
            Copy-Item -Recurse -Force tesseract dist/
            Write-Host "✅ Dossier Tesseract copié dans dist/"
          } else {
            Write-Host "⚠️ Aucun dossier Tesseract trouvé"
          }

          Write-Host ""
          Write-Host "🔍 Vérification du contenu de dist/"
          Get-ChildItem dist/

          Write-Host ""
          Write-Host "✅ Compilation Windows (Portable + Lite) terminée avec succès"

      - name: 🔍 Vérification post-build de Tesseract embarqué
        if: runner.os == 'Windows'
        shell: bash
        run: |
          if [ -d "dist/tesseract" ]; then
            echo "✅ Tesseract bien présent dans dist/"
            ls -la dist/tesseract
          else
            echo "❌ Tesseract manquant dans dist/"
            exit 1
          fi



      - name: 📦 Créer les packages de distribution (Windows, Linux, macOS)
        shell: bash
        run: |
          echo "📦 Préparation des packages de distribution..."
          
          # ==============================
          # 🪟 WINDOWS : Portable + Lite 
          # ==============================
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            cd dist
            
            # ---------- VERSION PORTABLE ----------
            if [ -f "GestionFinanciereLittle_Portable.exe" ]; then
              echo "📦 Création du package Windows Portable..."
              
              # Copier Tesseract embarqué depuis le dépôt
              if [ -d "../tesseract" ]; then
                echo "📂 Copie du dossier Tesseract depuis le dépôt..."
                cp -r ../tesseract ./
              fi
              
              7z a -tzip GestionFinanciereLittle-Windows-Portable.zip \
                GestionFinanciereLittle_Portable.exe *.py tesseract/
              
              echo "✅ Package Portable créé avec Tesseract embarqué"
            else
              echo "❌ Erreur : GestionFinanciereLittle_Portable.exe introuvable"
            fi
            
            # ---------- VERSION LITE ----------
            if [ -f "GestionFinanciereLittle_Lite.exe" ]; then
              echo "📦 Création du package Windows Lite..."
              
              # Copier installateur 
              if [ -f "../app/install_and_run_windows.ps1" ]; then
                cp ../app/install_and_run_windows.ps1 .
                echo "✅ Installateur PowerShell copié"
              else
                echo "⚠️ Fichier d'installation manquant"
              fi
              
              # Copier Tesseract embarqué (sécurité OCR)
              if [ -d "../tesseract" ]; then
                cp -r ../tesseract ./
                echo "✅ Dossier Tesseract embarqué ajouté dans la version Lite"
              fi
              
              7z a -tzip GestionFinanciereLittle-Windows-Lite.zip \
                GestionFinanciereLittle_Lite.exe *.py *.ps1 tesseract/
              
              echo "✅ Package Lite créé (Tesseract embarqué + installateur)"
            else
              echo "❌ Erreur : GestionFinanciereLittle_Lite.exe introuvable"
            fi
            
            cd ..
          fi
          
          
          # ==============================
          # 🐧 LINUX 
          # ==============================
          if [[ "$RUNNER_OS" == "Linux" ]]; then
            echo "📦 Création du package Linux..."
            mkdir -p dist/linux
            cp app/*.py dist/linux/
            cp -r tesseract dist/linux/ || echo "⚠️ Dossier Tesseract non trouvé (Linux)"  
            echo "#!/bin/bash" > dist/linux/run.sh
            echo "python3 -m streamlit run gestiolittle.py --server.headless true" >> dist/linux/run.sh
            chmod +x dist/linux/run.sh
            cd dist/linux
            zip -r ../GestionFinanciereLittle-Linux.zip ./
            cd ../..
            echo "✅ Package Linux créé avec succès (Tesseract inclus)"
          fi
          
          
          # ==============================
          # 🍎 MACOS
          # ==============================
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            echo "📦 Création du package macOS..."
            mkdir -p dist/macos
            cp app/*.py dist/macos/
            cp -r tesseract dist/macos/ || echo "⚠️ Dossier Tesseract non trouvé (macOS)"
            echo "#!/bin/bash" > dist/macos/run.sh
            echo "python3 -m streamlit run gestiolittle.py --server.headless true" >> dist/macos/run.sh
            chmod +x dist/macos/run.sh
            cd dist/macos
            zip -r ../GestionFinanciereLittle-macOS.zip ./
            cd ../..
            echo "✅ Package macOS créé avec succès (Tesseract inclus)"  
          fi
          
          
          echo ""
          echo "📁 Vérification du contenu final :"
          ls -la dist/*.zip || echo "⚠️ Aucun ZIP trouvé"
          echo ""
          echo "✅ Tous les packages de distribution ont été générés avec Tesseract embarqué"

      - name: 🔢 Lire la version depuis VERSION.txt
        id: ver
        run: |
          if [ ! -f version.txt ]; then
            echo "VERSION=0.0.0" >> $GITHUB_ENV
          else
            V=$(cat version.txt | tr -d ' \r\n')
            echo "VERSION=$V" >> $GITHUB_ENV
          fi
        shell: bash

      - name: 📝 Générer titre + notes (conventional commits)
        id: notes
        run: |
          set -e

          if git describe --tags --abbrev=0 >/tmp/last_tag 2>/dev/null; then
            LAST_TAG=$(cat /tmp/last_tag)
          else
            LAST_TAG=""
          fi
          if [ -n "$LAST_TAG" ]; then
            RANGE="${LAST_TAG}..HEAD"
          else
            RANGE="HEAD"
          fi

          # Récupérer les messages de commit
          git log --pretty=format:%s $RANGE > /tmp/commits.txt || true

          # Flags par type
          HAS_BREAK=$(grep -Ei '(^.*!|BREAKING CHANGE:)' /tmp/commits.txt || true)
          HAS_FEAT=$(grep -Ei '^feat(\(|:)' /tmp/commits.txt || true)
          HAS_FIX=$(grep -Ei '^fix(\(|:)' /tmp/commits.txt || true)
          HAS_PERF=$(grep -Ei '^perf(\(|:)' /tmp/commits.txt || true)
          HAS_REFACTOR=$(grep -Ei '^refactor(\(|:)' /tmp/commits.txt || true)
          HAS_DOCS=$(grep -Ei '^docs(\(|:)' /tmp/commits.txt || true)
          HAS_CHORE=$(grep -Ei '^chore(\(|:)' /tmp/commits.txt || true)

          # Titre dynamique
          TITLE="🔧 Maintenance"
          if [ -n "$HAS_BREAK" ]; then
            TITLE="💥 Changements majeurs"
          elif [ -n "$HAS_FEAT" ] && [ -n "$HAS_FIX" ]; then
            TITLE="✨ Nouvelles fonctionnalités & 🐛 Corrections"
          elif [ -n "$HAS_FEAT" ]; then
            TITLE="✨ Nouvelles fonctionnalités"
          elif [ -n "$HAS_FIX" ]; then
            TITLE="🐛 Corrections de bugs"
          elif [ -n "$HAS_PERF" ]; then
            TITLE="⚡ Améliorations de performances"
          elif [ -n "$HAS_REFACTOR" ]; then
            TITLE="🧹 Refactor"
          elif [ -n "$HAS_DOCS" ]; then
            TITLE="📝 Documentation"
          fi

          # Corps : regrouper par section
          body() {
            SEC="$1"; PAT="$2"
            MATCHES=$(grep -E "$PAT" /tmp/commits.txt | sed -E 's/^[a-z]+(\([^)]+\))?:\s*//' || true)
            if [ -n "$MATCHES" ]; then
              echo "### $SEC"
              echo "$MATCHES" | sed 's/^/- /'
              echo
            fi
          }

          {
            echo "## ${TITLE} — ${{ env.VERSION }}"
            echo
            [ -n "$HAS_BREAK" ] && echo "### 💥 Breaking changes" && \
              grep -Ei '(^.*!|BREAKING CHANGE:)' /tmp/commits.txt | sed 's/^/- /' && echo
            body "✨ Features" '^feat(\(|:)'
            body "🐛 Fixes" '^fix(\(|:)'
            body "⚡ Perf" '^perf(\(|:)'
            body "🧹 Refactor" '^refactor(\(|:)'
            body "📝 Docs" '^docs(\(|:)'
            body "🔧 Chore" '^chore(\(|:)'
          } > /tmp/release_notes.md

          echo "title=${TITLE}" >> $GITHUB_OUTPUT
        shell: bash

      - name: 📤 Upload de l'exécutable pour la Release (DRAFT)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.VERSION }}
          name: "Gestion Financière Little ${{ env.VERSION }} — ${{ steps.notes.outputs.title }}"
          draft: true
          prerelease: false
          body_path: /tmp/release_notes.md
          files: |
            dist/GestionFinanciereLittle*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
